# Core scientific stack
numpy>=1.26,<2.0
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5

# FastText (pre-built wheel; avoids needing a C++ compiler on target)
fasttext-wheel>=0.9.2

# PyTorch — install separately before this file to pin the CPU-only wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# The Dockerfile handles this automatically; for local installs see the README.

# NLP / sequence labelling
seqeval>=1.2.2
# pytorch-crf provides `torchcrf` (preferred); TorchCRF is the fallback kept for compat
pytorch-crf>=0.7.2

# Visualisation
matplotlib>=3.8
seaborn>=0.13
